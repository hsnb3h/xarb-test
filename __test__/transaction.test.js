// const { expect } = require("chai")
jest.useFakeTimers()
const dotenv = require("dotenv").config();
const { User } = require("../models/model")


describe("Do Some stuff", () => {
    it("My Test Case", () => {
        
    })
})